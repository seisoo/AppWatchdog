name: Publish & Release (Windows)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: self-hosted

    steps:
      # 1️⃣ Checkout (FULL history required for tagging)
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Read version from project
      - name: Read project version
        id: version
        shell: powershell
        run: |
          [xml]$props = Get-Content Directory.Build.props
          $version = $props.Project.PropertyGroup.Version
          if (-not $version) {
            Write-Error "Version not found in Directory.Build.props"
            exit 1
          }

          echo "VERSION=$version" >> $env:GITHUB_ENV
          echo "TAG=v$version" >> $env:GITHUB_ENV

      # 3️⃣ Ensure tag does NOT already exist
      - name: Delete tag if it exists (local + remote)
        shell: powershell
        run: |
            git fetch --tags
        
            if (git tag -l "${{ env.TAG }}") {
              Write-Host "Tag ${{ env.TAG }} exists – deleting it"
        
              # delete local tag
              git tag -d "${{ env.TAG }}"
        
              # delete remote tag
              git push origin ":refs/tags/${{ env.TAG }}"
            } else {
              Write-Host "Tag ${{ env.TAG }} does not exist – creating new"
            }


      # 4️⃣ Create and push tag
      - name: Create and push tag
        shell: powershell
        run: |
          git tag "${{ env.TAG }}"
          git push origin "${{ env.TAG }}"

      # 5️⃣ Restore
      - name: Restore
        run: dotnet restore AppWatchdog.sln

      # =========================
      # 🔹 BUILD ALL TARGETS
      # =========================
      - name: Publish Service + UI (all RIDs)
        shell: powershell
        run: |
          $targets = @("win-x64", "win-x86", "win-arm64")

          foreach ($t in $targets) {
            dotnet publish AppWatchdog.Service/AppWatchdog.Service.csproj `
              -c Release -r $t --self-contained true `
              /p:PublishSingleFile=true `
              /p:PublishTrimmed=false `
              /p:IncludeNativeLibrariesForSelfExtract=true

            dotnet publish AppWatchdog.UI.WPF/AppWatchdog.UI.WPF.csproj `
              -c Release -r $t --self-contained true `
              /p:PublishSingleFile=true `
              /p:PublishTrimmed=false `
              /p:IncludeNativeLibrariesForSelfExtract=true
          }

      # =========================
      # 📦 COLLECT + ZIP
      # =========================
      - name: Collect Outputs + ZIPs
        shell: powershell
        run: |
          $targets = @("win-x64", "win-x86", "win-arm64")

          foreach ($t in $targets) {
            $outDir = "release\$t"
            New-Item -ItemType Directory -Force $outDir | Out-Null

            Copy-Item `
              "AppWatchdog.Service/bin/Release/net8.0-windows/$t/publish/AppWatchdog.Service.exe" `
              $outDir -Force

            Copy-Item `
              "AppWatchdog.UI.WPF/bin/Release/net8.0-windows/$t/publish/AppWatchdog.UI.WPF.exe" `
              $outDir -Force

            Compress-Archive `
              -Path "$outDir\*" `
              -DestinationPath "AppWatchdog_${{ env.TAG }}_$t.zip" `
              -Force
          }

      - name: Generate changelog
        shell: powershell
        run: |
          git fetch --tags
      
          $prevTag = git tag --sort=-creatordate | Select-Object -Skip 1 -First 1
          if (-not $prevTag) {
            $log = git log --pretty=format:"- %s"
          } else {
            $log = git log "$prevTag..HEAD" --pretty=format:"- %s"
          }
      
          $log | Out-File changelog.txt -Encoding utf8


      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: AppWatchdog ${{ env.TAG }}
          body_path: changelog.txt
          replace_existing_tag: true
          files: |
            AppWatchdog_${{ env.TAG }}_win-x64.zip
            AppWatchdog_${{ env.TAG }}_win-x86.zip
            AppWatchdog_${{ env.TAG }}_win-arm64.zip


