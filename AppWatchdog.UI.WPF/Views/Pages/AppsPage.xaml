<Page
    x:Class="AppWatchdog.UI.WPF.Views.Pages.AppsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:AppWatchdog.UI.WPF.Common"
    xmlns:conv="clr-namespace:AppWatchdog.UI.WPF.Converters"
    xmlns:loc="clr-namespace:AppWatchdog.UI.WPF.Localization"
    xmlns:sharedMon="clr-namespace:AppWatchdog.Shared.Monitoring;assembly=AppWatchdog.Shared"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Page.Resources>

        <!--  Enum values for WatchTargetType  -->
        <x:Array x:Key="WatchTargetTypeValues" Type="{x:Type sharedMon:WatchTargetType}">
            <sharedMon:WatchTargetType>Executable</sharedMon:WatchTargetType>
            <sharedMon:WatchTargetType>WindowsService</sharedMon:WatchTargetType>
            <sharedMon:WatchTargetType>HttpEndpoint</sharedMon:WatchTargetType>
            <sharedMon:WatchTargetType>TcpPort</sharedMon:WatchTargetType>
        </x:Array>

        <!--  Simple style to show/hide blocks based on SelectedApp.Type without converters  -->
        <Style x:Key="TargetBlockStyle" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <!--  default: Collapsed  -->
            </Style.Triggers>
        </Style>

    </Page.Resources>

    <Grid IsEnabled="{Binding IsContentEnabled}">
        <StackPanel Margin="16">

            <!--  HEADER  -->
            <ui:Card
                Height="154"
                Margin="0,0,0,16"
                Padding="0">
                <Grid>

                    <Image Source="pack://application:,,,/Images/banner5.png" Stretch="UniformToFill" />

                    <Rectangle>
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Offset="0" Color="#AA000000" />
                                <GradientStop Offset="1" Color="#AA000000" />
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>

                    <StackPanel Margin="24,36,24,36" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="26"
                            FontWeight="SemiBold"
                            Foreground="White"
                            Text="AppWatchdog" />
                        <TextBlock
                            Margin="0,10,0,0"
                            Foreground="#E5E7EB"
                            Text="天網恢恢、疎にして漏らさず" />
                    </StackPanel>

                </Grid>
            </ui:Card>

            <!--  SETTINGS  -->
            <ui:Card Margin="0,0,0,16">
                <StackPanel>

                    <TextBlock
                        FontSize="20"
                        FontWeight="SemiBold"
                        Text="{Binding Source={StaticResource Strings}, Path=[apps_settings_title]}" />

                    <TextBlock
                        Margin="0,4,0,0"
                        Opacity="0.7"
                        Text="{Binding HintText}" />

                    <WrapPanel Margin="0,20,0,0">
                        <ui:Button
                            Margin="0,0,8,0"
                            Command="{Binding ReloadCommand}"
                            Content="{Binding Source={StaticResource Strings}, Path=[apps_reload]}"
                            Icon="{ui:SymbolIcon Symbol=SparkleCircle24}" />

                        <ui:Button
                            Appearance="Primary"
                            Command="{Binding SaveCommand}"
                            Content="{Binding Source={StaticResource Strings}, Path=[save]}"
                            Icon="{ui:SymbolIcon Symbol=Save24}"
                            IsEnabled="{Binding IsDirty}" />
                    </WrapPanel>

                    <TextBlock
                        Margin="0,4,0,0"
                        HorizontalAlignment="Right"
                        FontFamily="Consolas"
                        Foreground="NavajoWhite"
                        Text="{Binding SaveStateText}" />

                </StackPanel>
            </ui:Card>

            <!--  GENERAL  -->
            <ui:Card Margin="0,0,0,16">
                <Grid Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="260" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="1"
                        Grid.ColumnSpan="4"
                        Margin="0,0,0,8"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="{Binding Source={StaticResource Strings}, Path=[apps_general_monitoring]}" />

                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        Margin="8,8,8,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Text="{Binding Source={StaticResource Strings}, Path=[apps_check_interval]}" />

                    <ui:NumberBox
                        Grid.Row="2"
                        Grid.Column="1"
                        Width="auto"
                        HorizontalAlignment="left"
                        Maximum="1440"
                        Minimum="1"
                        Value="{Binding CheckIntervalSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="2"
                        Margin="8,8,8,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Text="{Binding Source={StaticResource Strings}, Path=[apps_mail_interval]}" />

                    <ui:NumberBox
                        Grid.Row="2"
                        Grid.Column="3"
                        Width="auto"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Left"
                        Maximum="168"
                        Minimum="0"
                        Value="{Binding MailIntervalHours, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </ui:Card>

            <!--  APPS  -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  LEFT  -->
                <ui:Card Grid.Column="0" VerticalAlignment="Top">
                    <StackPanel>

                        <TextBlock
                            FontSize="16"
                            FontWeight="SemiBold"
                            Text="{Binding Source={StaticResource Strings}, Path=[apps_watched_apps_title]}" />

                        <TextBlock
                            Margin="0,5,0,5"
                            FontFamily="Consolas"
                            FontSize="11"
                            Opacity="0.7"
                            Text="{Binding Source={StaticResource Strings}, Path=[apps_rename_hint]}" />

                        <WrapPanel Margin="10,10,5,0" HorizontalAlignment="Right">
                            <ui:Button
                                Margin="0,0,8,0"
                                Appearance="Primary"
                                Command="{Binding AddCommand}"
                                Content="{Binding Source={StaticResource Strings}, Path=[apps_add]}"
                                Icon="{ui:SymbolIcon Symbol=Add24}" />

                            <ui:Button
                                Appearance="Secondary"
                                Command="{Binding RemoveCommand}"
                                Content="{Binding Source={StaticResource Strings}, Path=[apps_remove]}"
                                Icon="{ui:SymbolIcon Symbol=Delete24}"
                                IsEnabled="{Binding HasSelectedApp}" />
                        </WrapPanel>

                        <Separator Margin="0,5" />

                        <ui:ListView
                            Height="317"
                            ItemsSource="{Binding Apps}"
                            MouseDoubleClick="AppsListView_MouseDoubleClick"
                            ScrollViewer.VerticalScrollBarVisibility="Visible"
                            SelectedItem="{Binding SelectedApp}">

                            <ui:ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10,8">
                                        <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                        <TextBlock
                                            FontSize="11"
                                            Opacity="0.7"
                                            Text="{Binding Type}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ui:ListView.ItemTemplate>

                        </ui:ListView>

                    </StackPanel>
                </ui:Card>

                <!--  RIGHT  -->
                <ui:Card
                    Grid.Column="2"
                    VerticalAlignment="Top"
                    IsEnabled="{Binding HasSelectedApp}">
                    <ScrollViewer>
                        <StackPanel DataContext="{Binding SelectedApp}">

                            <TextBlock
                                Margin="0,0,0,12"
                                FontSize="16"
                                FontWeight="SemiBold"
                                Text="{Binding Source={StaticResource Strings}, Path=[apps_monitoring_title]}" />

                            <!--  Enable  -->
                            <ui:ToggleSwitch Content="{Binding Source={StaticResource Strings}, Path=[apps_monitoring_enable]}" IsChecked="{Binding Enabled}" />

                            <!--  Target Type  -->
                            <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Enabled}">
                                <TextBlock Text="Target Type" />
                                <ComboBox
                                    Margin="0,4,0,0"
                                    ItemsSource="{StaticResource WatchTargetTypeValues}"
                                    SelectedItem="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  =========================  -->
                            <!--  EXECUTABLE  -->
                            <!--  =========================  -->
                            <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Enabled}">
                                <StackPanel.Style>
                                    <Style BasedOn="{StaticResource TargetBlockStyle}" TargetType="{x:Type StackPanel}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="{x:Static sharedMon:WatchTargetType.Executable}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=[apps_exe_path]}" />

                                <Grid Margin="0,4,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <ui:TextBox Text="{Binding ExePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                    <ui:Button
                                        Grid.Column="2"
                                        Command="{Binding BrowseExeCommand}"
                                        Icon="{ui:SymbolIcon Symbol=Folder24}"
                                        ToolTip="{Binding Source={StaticResource Strings}, Path=[apps_select_exe]}" />
                                </Grid>

                                <TextBlock Text="{Binding Source={StaticResource Strings}, Path=[apps_arguments]}" />
                                <ui:TextBox Margin="0,4,0,0" Text="{Binding Arguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  =========================  -->
                            <!--  WINDOWS SERVICE  -->
                            <!--  =========================  -->
                            <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Enabled}">
                                <StackPanel.Style>
                                    <Style BasedOn="{StaticResource TargetBlockStyle}" TargetType="{x:Type StackPanel}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="{x:Static sharedMon:WatchTargetType.WindowsService}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="Service name" />
                                <ui:TextBox Margin="0,4,0,0" Text="{Binding ServiceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  =========================  -->
                            <!--  HTTP ENDPOINT  -->
                            <!--  =========================  -->
                            <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Enabled}">
                                <StackPanel.Style>
                                    <Style BasedOn="{StaticResource TargetBlockStyle}" TargetType="{x:Type StackPanel}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="{x:Static sharedMon:WatchTargetType.HttpEndpoint}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="URL" />
                                <ui:TextBox
                                    BorderBrush="{Binding IsUrlValid, Converter={StaticResource BooleanToBrushConverter}}"
                                    Text="{Binding Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    ToolTip="Must be a valid absolute URL (http/https)" />


                                <TextBlock Text="Expected HTTP status code" />
                                <ui:NumberBox
                                    Margin="0,4,0,0"
                                    Maximum="599"
                                    Minimum="100"
                                    Value="{Binding ExpectedStatusCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  =========================  -->
                            <!--  TCP PORT  -->
                            <!--  =========================  -->
                            <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Enabled}">
                                <StackPanel.Style>
                                    <Style BasedOn="{StaticResource TargetBlockStyle}" TargetType="{x:Type StackPanel}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="{x:Static sharedMon:WatchTargetType.TcpPort}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock Text="Host" />
                                <ui:TextBox Margin="0,4,0,8" Text="{Binding Host, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBlock Text="Port" />
                                <ui:NumberBox
                                    Margin="0,4,0,0"
                                    Maximum="65535"
                                    Minimum="1"
                                    Value="{Binding Port, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  =========================  -->
                            <!--  KUMA (Executable only)  -->
                            <!--  =========================  -->
                            <StackPanel Margin="0,12,0,0">
                                <StackPanel.Style>
                                    <Style BasedOn="{StaticResource TargetBlockStyle}" TargetType="{x:Type StackPanel}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="{x:Static sharedMon:WatchTargetType.Executable}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <ui:ToggleSwitch
                                    Content="{Binding Source={StaticResource Strings}, Path=[apps_kuma_enable]}"
                                    IsChecked="{Binding KumaEnabled}"
                                    IsEnabled="{Binding Enabled}" />

                                <StackPanel Margin="0,12,0,0" IsEnabled="{Binding KumaEnabled}">

                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=[apps_kuma_base_url]}" />
                                    <ui:TextBox Margin="0,4,0,8" Text="{Binding KumaBaseUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=[apps_kuma_token]}" />
                                    <ui:TextBox Margin="0,4,0,8" Text="{Binding KumaPushToken, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                    <TextBlock Text="{Binding Source={StaticResource Strings}, Path=[apps_kuma_interval]}" />
                                    <ui:NumberBox
                                        Margin="0,4,0,0"
                                        Maximum="3600"
                                        Minimum="10"
                                        Value="{Binding KumaIntervalSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </StackPanel>

                        </StackPanel>
                    </ScrollViewer>
                </ui:Card>

            </Grid>

        </StackPanel>
    </Grid>
</Page>
